"""Define version identifiers for the local tender extraction package.

VERSION, SCHEMA_VERSION, and PROMPT_VERSION identify the implementation and its
contracts in run manifests and cache fingerprints. This module has no callable
entry point and performs no file processing or cloud calls when imported.
"""
VERSION = "0.1.0"
SCHEMA_VERSION = "3"
PROMPT_VERSION = "6"
